"use client";
import React from "react";
import UniversitySideBar from "@/components/university/UniversitySideBar"
export default function TeacherLayout({ children }) {
  
  return <UniversitySideBar>{children}</UniversitySideBar>;
}
